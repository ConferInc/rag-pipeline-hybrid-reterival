# Test Layout

This folder is organized to mirror production modules and make ownership clear.

- `tests/api/`
- `tests/augmentation/`
- `tests/chatbot/`
- `tests/embeddings/`
- `tests/orchestrator/`
- `tests/retrieval/`
- `tests/profile/`
- `tests/validation/`
- `tests/utils/`

Existing tests can stay at the root while migration is in progress.
